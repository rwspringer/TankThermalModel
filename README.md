# TankThermalModel

Code to simulate the thermal behavior of water tanks at various locations using weather data from meteo.com.

This repository can also be used to store SWGO collaboration assets related to the thermal model:

- Python scripts in `/scripts/python/`
- Bash scripts in `/scripts/bash/`
- Thermal-model output directories in `/outputs/`
